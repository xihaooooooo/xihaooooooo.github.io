判题字面意思就是判断题目对错，但是这个模块并只是判断对错这么简单。它与后面的一系列操作息息相关。
判题的输入就是题目，学生答案，参考答案。但是为了后面的操作，它通常还要加其他东西。
判题的输出是答案正确与否，判题理由。如果答案错误，还会加上错因标签。

判题的时候会进行路由，如果未作答直接判错且直接标记为记忆不清。如果是选择题使用规则判题，答案错了再使用LLM进行错因判断。填空和简答题则直接使用LLM进行语义判断和错因分析

当然判题不可能是一道一道判，语义判题是一次性最多同时执行5道，如果是错因判断则一次性最多执行2道。其实错因判断里面就只是选择题，因为语义判断的时候可以顺便判断错因
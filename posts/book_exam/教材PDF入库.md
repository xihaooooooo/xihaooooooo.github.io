把用户上传的教材保存到本地目录里面。先写到临时目录里面，等完整上传后再转到正式目录里面

解析PDF使用了MinerU
先申请上传地址，再给上传地址上传PDF，得到一个batch_id,可以通过batch_id去查询，如果状态为done则把它们下载为zip。再进行解压
这里有个坑，MinerU每次只支持200页解析，但是教材基本上都超过200页，所以得进行分片处理。
分片处理就是把每个PDF切分为200页大小的新PDF存放到目录里面
再把之前的上传，得到状态，下载zip，解压zip加一个多批次版本
把解压zip得到的内容解析成一个对象，里面存放的是页码和这一页的内容
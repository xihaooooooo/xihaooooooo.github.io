模板查询这个东西，在很多地方都会用到，比如领卷下单锁卷，商家查模板的时候。它们都调用的是这一个地方，不需要自己实现逻辑。

查询缓存有两种结果，一种是命中，一种是没命中。
如果命中的话，那么它就把Hash里的内容反序列化，就得到了模板的值。
如果未命中，就开始经过布隆过滤器，缓存空值，双重判定锁，查库，回写到缓存里面。如果查库为空，就写空值在那个key上面，等下一次查缓存为空就可以直接返回卷不存在。

布隆过滤器只会过滤不存在，它虽然会误判，但它不会把存在的误判成不存在，模板不会丢失

回写这里使用了Lua脚本，因为它有两步，一步是设置Hash值，一步是设置过期时间